name: Auto-Create PR from main to prod
on:
    push:
        branches:
            - dev

jobs:
    create-pr:
        if: "github.event.head_commit != null && !contains(github.event.head_commit.message, 'Merge pull request') && !contains(github.event.head_commit.message, 'Merge branch')"
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Create Pull Request
              env:
                  GITHUB_TOKEN: ${{ secrets.JULES_AUTOMATION_PAT }}
              run: |
                  gh pr create --base prod --head main --title "review" --body "Automated PR from main to prod." || echo "PR already exists or could not be created"
